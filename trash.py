    array = ["Grass Mowing" , "Shopping", "Study"]
    Todo ="To Do"
    Completed = "Completed"
    ToCo =[]
    if cijfer = 1
        for item in array:
            print('-' , item)
    
    if cijfer = 2
    
        taak = prompt('What do you want to add?')
        array.append(taak) 

        for element in array:
            print("-" , element)
        
        
    }


    if cijfer = 3
    
        taak = prompt('What do you want to delete?'):

        for i = 0: 
    
            if  array[i] = taak: 
        
                array.remove(i):
            
        
        }

        for element in array (
            print('-' , element)
        });
    }

    if (cijfer =4) {
        taak = prompt('What do you want to mark as done?');

        for( var i = 0; i < array.length; i++){ 
    
            if ( array[i] = taak) { 
        
                mario = taak + "-" + Completed 
                array[i] = mario
            }
            else {
                mario = array[i] + '-' + Todo
                array[i] = mario
            }
        
        }

        for element in array (
            print('-' , element)
    }

    if (cijfer = 5)
        (
        print('You quit the task manager');
